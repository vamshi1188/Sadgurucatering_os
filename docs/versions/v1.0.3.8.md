============================================================
SADGURU CATERING OS
v1.0.3.8 — STRUCTURED LOGGING
============================================================

OBJECTIVE
---------
Build the complete structured logging foundation using Go's
standard library log/slog.

After this version:

HTTP/Application
       |
       v
   Logger
       |
       v
Structured JSON -> stdout -> Docker/Cloud logging

Supported levels:
    debug
    info
    warn
    error

Default:
    info


VERSION TREE
------------

v1.0.3.8
│
├── v1.0.3.8.1   Logger package
├── v1.0.3.8.2   Log levels
├── v1.0.3.8.3   Structured JSON logging
├── v1.0.3.8.4   Logger configuration
├── v1.0.3.8.5   Application logger integration
├── v1.0.3.8.6   HTTP request logging API
├── v1.0.3.8.7   Error logging integration
├── v1.0.3.8.8   Logger tests
├── v1.0.3.8.9   Runtime verification
└── v1.0.3.8.10  Validate and close


============================================================
PRE-CHECK
============================================================

cd ~/workspace/BUSINESS/CATERING_OS/Sadgurucatering_os

git branch --show-current

Expected:
feature/v1.0.3-go-backend-foundation

git status

Make sure there are no unexpected changes.


============================================================
v1.0.3.8.1 — LOGGER PACKAGE
============================================================

ACTION:
Create the logger package.

COMMAND:

mkdir -p backend/internal/logger

Create:

backend/internal/logger/logger.go

ACTION:
Replace the ENTIRE file with:

------------------------------------------------------------
package logger

import (
	"io"
	"log/slog"
	"os"
)

type Logger struct {
	*slog.Logger
}

func New(w io.Writer) *Logger {
	if w == nil {
		w = os.Stdout
	}

	return &Logger{
		Logger: slog.New(
			slog.NewJSONHandler(w, nil),
		),
	}
}
------------------------------------------------------------

Run:

gofmt -w backend/internal/logger/logger.go

go -C backend test ./...

go -C backend vet ./...

DONE WHEN:

[ ] logger directory exists
[ ] logger.go exists
[ ] Logger type exists
[ ] New() exists
[ ] gofmt passes
[ ] tests pass
[ ] vet passes


============================================================
v1.0.3.8.2 — LOG LEVELS
============================================================

ACTION:
Modify:

backend/internal/logger/logger.go

ACTION:
Replace the ENTIRE file with:

------------------------------------------------------------
package logger

import (
	"io"
	"log/slog"
	"os"
)

type Logger struct {
	*slog.Logger
}

func New(w io.Writer) *Logger {
	if w == nil {
		w = os.Stdout
	}

	options := &slog.HandlerOptions{
		Level: slog.LevelInfo,
	}

	return &Logger{
		Logger: slog.New(
			slog.NewJSONHandler(w, options),
		),
	}
}
------------------------------------------------------------

Run:

gofmt -w backend/internal/logger/logger.go

go -C backend test ./...

go -C backend vet ./...

DONE WHEN:

[ ] DEBUG supported by slog
[ ] INFO supported
[ ] WARN supported
[ ] ERROR supported
[ ] default level is INFO
[ ] tests pass


============================================================
v1.0.3.8.3 — STRUCTURED JSON LOGGING
============================================================

ACTION:
Create:

backend/internal/logger/logger_test.go

ACTION:
Replace the ENTIRE file with:

------------------------------------------------------------
package logger

import (
	"bytes"
	"encoding/json"
	"testing"
)

func TestLoggerProducesJSON(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer)

	log.Info("application started")

	var entry map[string]any

	if err := json.Unmarshal(buffer.Bytes(), &entry); err != nil {
		t.Fatalf("expected valid JSON log: %v", err)
	}

	if entry["msg"] != "application started" {
		t.Fatalf("unexpected message: %v", entry["msg"])
	}

	if entry["level"] != "INFO" {
		t.Fatalf("unexpected level: %v", entry["level"])
	}
}
------------------------------------------------------------

Run:

gofmt -w backend/internal/logger/logger_test.go

go -C backend test ./...

DONE WHEN:

[ ] logger produces valid JSON
[ ] level is present
[ ] message is present
[ ] test passes


============================================================
v1.0.3.8.4 — LOGGER CONFIGURATION
============================================================

We will add:

LOG_LEVEL=info

to the existing configuration.

------------------------------------------------------------
FILE:
backend/internal/config/config.go
------------------------------------------------------------

ACTION:
DO NOT replace the entire file.

Find your existing:

type Config struct {
	...
}

Add this field inside the struct:

	LogLevel string

Example:

type Config struct {
	App         AppConfig
	HTTP        HTTPConfig
	Database    DatabaseConfig
	Redis       RedisConfig
	Storage     StorageConfig
	Environment string
	LogLevel    string
}

IMPORTANT:
Keep all your existing fields.

------------------------------------------------------------
SAME FILE:
backend/internal/config/config.go
------------------------------------------------------------

Find the place where your Config object is initialized:

cfg := Config{
	...
}

Inside that Config literal add:

	LogLevel: getEnv("LOG_LEVEL", "info"),

Example:

cfg := Config{
	Environment: getEnv("APP_ENV", "development"),
	LogLevel:    getEnv("LOG_LEVEL", "info"),

	// KEEP YOUR EXISTING CONFIGURATION HERE
}

Do not delete existing configuration.

------------------------------------------------------------
FILE:
.env.example
------------------------------------------------------------

Find:

APP_VERSION=1.0.3

Add immediately below:

LOG_LEVEL=info

Example:

APP_ENV=development
APP_NAME=Sadguru Catering OS
APP_VERSION=1.0.3
LOG_LEVEL=info

Keep all other existing variables.

Run:

gofmt -w backend/internal/config/config.go

go -C backend test ./...

go -C backend vet ./...

DONE WHEN:

[ ] Config contains LogLevel
[ ] LOG_LEVEL is loaded
[ ] default is info
[ ] .env.example documents LOG_LEVEL
[ ] tests pass


============================================================
v1.0.3.8.5 — APPLICATION LOGGER INTEGRATION
============================================================

FILE:
backend/internal/logger/logger.go

ACTION:
Replace ENTIRE file with:

------------------------------------------------------------
package logger

import (
	"io"
	"log/slog"
	"os"
	"strings"
)

type Logger struct {
	*slog.Logger
}

func New(w io.Writer, level string) *Logger {
	if w == nil {
		w = os.Stdout
	}

	logLevel := parseLevel(level)

	options := &slog.HandlerOptions{
		Level: logLevel,
	}

	return &Logger{
		Logger: slog.New(
			slog.NewJSONHandler(w, options),
		),
	}
}

func parseLevel(level string) slog.Level {
	switch strings.ToLower(level) {
	case "debug":
		return slog.LevelDebug

	case "warn", "warning":
		return slog.LevelWarn

	case "error":
		return slog.LevelError

	default:
		return slog.LevelInfo
	}
}
------------------------------------------------------------

IMPORTANT:
New() has changed from:

New(writer)

to:

New(writer, level)

Therefore existing calls to New() must be updated.

------------------------------------------------------------
FILE:
backend/internal/logger/logger_test.go
------------------------------------------------------------

ACTION:
Replace ENTIRE file with:

------------------------------------------------------------
package logger

import (
	"bytes"
	"encoding/json"
	"testing"
)

func TestLoggerProducesJSON(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "info")

	log.Info("application started")

	var entry map[string]any

	if err := json.Unmarshal(buffer.Bytes(), &entry); err != nil {
		t.Fatalf("expected valid JSON log: %v", err)
	}

	if entry["msg"] != "application started" {
		t.Fatalf("unexpected message: %v", entry["msg"])
	}

	if entry["level"] != "INFO" {
		t.Fatalf("unexpected level: %v", entry["level"])
	}
}

func TestLoggerLevel(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "error")

	log.Info("this should not appear")

	if buffer.Len() != 0 {
		t.Fatal("expected INFO log to be filtered at ERROR level")
	}

	log.Error("this should appear")

	if buffer.Len() == 0 {
		t.Fatal("expected ERROR log to be written")
	}
}
------------------------------------------------------------

------------------------------------------------------------
FILE:
backend/internal/app/app.go
------------------------------------------------------------

ACTION:
This is an existing file.

DO NOT replace the entire file.

1. Find the import block.

Add:

	"github.com/vamshi1188/Sadgurucatering_os/backend/internal/logger"

Your import block should contain logger alongside your existing
internal packages.

2. Find:

type App struct {

Add:

	Logger *logger.Logger

Your structure should look approximately like:

type App struct {
	Config     config.Config
	Logger     *logger.Logger
	HTTPServer *httpserver.Server
}

KEEP your existing fields.

3. Find:

func New(cfg config.Config) *App {

Replace ONLY this function with:

------------------------------------------------------------
func New(cfg config.Config) *App {
	handler := router.New()

	appLogger := logger.New(nil, cfg.LogLevel)

	return &App{
		Config: cfg,
		Logger: appLogger,
		HTTPServer: httpserver.New(
			cfg.HTTP.Host,
			cfg.HTTP.Port,
			handler,
		),
	}
}
------------------------------------------------------------

4. Find:

func (a *App) Run() error {

Replace ONLY this function with:

------------------------------------------------------------
func (a *App) Run() error {
	a.Logger.Info(
		"application starting",
		"app", a.Config.App.Name,
		"version", a.Config.App.Version,
		"environment", a.Config.Environment,
		"host", a.Config.HTTP.Host,
		"port", a.Config.HTTP.Port,
	)

	return a.HTTPServer.Start()
}
------------------------------------------------------------

Do not change anything else in app.go.

Run:

gofmt -w backend/internal/app/app.go
gofmt -w backend/internal/logger/*.go

go -C backend test ./...

go -C backend vet ./...

DONE WHEN:

[ ] App contains Logger
[ ] Logger initialized from cfg.LogLevel
[ ] Application startup is logged
[ ] tests pass
[ ] vet passes


============================================================
v1.0.3.8.6 — HTTP REQUEST LOGGING API
============================================================

IMPORTANT:
We are NOT creating HTTP middleware yet.

Middleware belongs to:

v1.0.3.9

Here we only create the logger capability that middleware
will use later.

FILE:
backend/internal/logger/logger.go

ACTION:
Add this function at the VERY BOTTOM of the existing file:

------------------------------------------------------------
func (l *Logger) Request(
	method string,
	path string,
	status int,
	durationMS float64,
) {
	l.Info(
		"http request",
		"method", method,
		"path", path,
		"status", status,
		"duration_ms", durationMS,
	)
}
------------------------------------------------------------

Do not change the existing functions.

Run:

gofmt -w backend/internal/logger/logger.go

DONE WHEN:

[ ] Request() exists
[ ] method is logged
[ ] path is logged
[ ] status is logged
[ ] duration_ms is logged


============================================================
v1.0.3.8.7 — ERROR LOGGING INTEGRATION
============================================================

FILE:
backend/internal/logger/logger.go

ACTION:
Add this function at the VERY BOTTOM:

------------------------------------------------------------
func (l *Logger) ErrorWithCause(
	message string,
	err error,
) {
	if err == nil {
		l.Error(message)
		return
	}

	l.Error(
		message,
		"error", err.Error(),
	)
}
------------------------------------------------------------

This logs internal error details.

IMPORTANT:
This function is for LOGGING.

It must NOT be used to directly construct API responses.

The API response continues to use:

response.WriteError(...)

from v1.0.3.7.

Run:

gofmt -w backend/internal/logger/logger.go

go -C backend test ./...

go -C backend vet ./...

DONE WHEN:

[ ] ErrorWithCause exists
[ ] nil errors are handled
[ ] underlying error can be logged
[ ] tests pass


============================================================
v1.0.3.8.8 — LOGGER TESTS
============================================================

FILE:
backend/internal/logger/logger_test.go

ACTION:
Replace ENTIRE file with this final test file:

------------------------------------------------------------
package logger

import (
	"bytes"
	"encoding/json"
	"errors"
	"testing"
)

func TestLoggerProducesJSON(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "info")

	log.Info("application started")

	var entry map[string]any

	if err := json.Unmarshal(buffer.Bytes(), &entry); err != nil {
		t.Fatalf("expected valid JSON log: %v", err)
	}

	if entry["msg"] != "application started" {
		t.Fatalf("unexpected message: %v", entry["msg"])
	}

	if entry["level"] != "INFO" {
		t.Fatalf("unexpected level: %v", entry["level"])
	}
}

func TestLoggerLevel(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "error")

	log.Info("this should not appear")

	if buffer.Len() != 0 {
		t.Fatal("expected INFO log to be filtered at ERROR level")
	}

	log.Error("this should appear")

	if buffer.Len() == 0 {
		t.Fatal("expected ERROR log to be written")
	}
}

func TestRequestLogging(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "info")

	log.Request(
		"GET",
		"/api/v1/customers",
		200,
		12.5,
	)

	var entry map[string]any

	if err := json.Unmarshal(buffer.Bytes(), &entry); err != nil {
		t.Fatalf("expected valid JSON log: %v", err)
	}

	if entry["msg"] != "http request" {
		t.Fatalf("unexpected message: %v", entry["msg"])
	}

	if entry["method"] != "GET" {
		t.Fatalf("unexpected method: %v", entry["method"])
	}

	if entry["path"] != "/api/v1/customers" {
		t.Fatalf("unexpected path: %v", entry["path"])
	}

	if entry["status"] != float64(200) {
		t.Fatalf("unexpected status: %v", entry["status"])
	}
}

func TestErrorWithCause(t *testing.T) {
	var buffer bytes.Buffer

	log := New(&buffer, "error")

	log.ErrorWithCause(
		"database operation failed",
		errors.New("connection refused"),
	)

	var entry map[string]any

	if err := json.Unmarshal(buffer.Bytes(), &entry); err != nil {
		t.Fatalf("expected valid JSON log: %v", err)
	}

	if entry["msg"] != "database operation failed" {
		t.Fatalf("unexpected message: %v", entry["msg"])
	}

	if entry["error"] != "connection refused" {
		t.Fatalf("unexpected error: %v", entry["error"])
	}
}
------------------------------------------------------------

Run:

gofmt -w backend/internal/logger/logger_test.go

go -C backend test ./... -v

Expected:

All tests PASS.


============================================================
v1.0.3.8.9 — RUNTIME VERIFICATION
============================================================

First build:

go -C backend build ./...

Start:

go -C backend run ./cmd/api

Expected:
A JSON startup log similar to:

{
  "time": "...",
  "level": "INFO",
  "msg": "application starting",
  "app": "Sadguru Catering OS",
  "version": "1.0.3",
  "environment": "development",
  "host": "0.0.0.0",
  "port": "8080"
}

The timestamp will differ.

Stop:

Ctrl+C


TEST LOG LEVEL:

Run:

LOG_LEVEL=error go -C backend run ./cmd/api

The INFO startup message should NOT appear.

Stop:

Ctrl+C


Run:

LOG_LEVEL=debug go -C backend run ./cmd/api

Application should start successfully.

Stop:

Ctrl+C


============================================================
v1.0.3.8.10 — FINAL VALIDATION
============================================================

Run all commands in this order.

1. FORMAT

gofmt -w \
  backend/internal/logger/*.go \
  backend/internal/config/config.go \
  backend/internal/app/app.go


2. TEST

go -C backend test ./...


3. VET

go -C backend vet ./...


4. BUILD

go -C backend build ./...


5. FORMAT CHECK

gofmt -d \
  backend/internal/logger/*.go \
  backend/internal/config/config.go \
  backend/internal/app/app.go

Expected:
NO OUTPUT


6. GIT CHECK

git diff --check

Expected:
NO OUTPUT


7. STATUS

git status

Review all changes.


============================================================
FINAL FILE STRUCTURE
============================================================

backend/
└── internal/
    ├── app/
    │   └── app.go                  UPDATED
    │
    ├── config/
    │   └── config.go               UPDATED
    │
    ├── logger/
    │   ├── logger.go               CREATED
    │   └── logger_test.go          CREATED
    │
    ├── errors/
    │   └── existing
    │
    ├── response/
    │   └── existing
    │
    ├── httpserver/
    │   └── existing
    │
    └── router/
        └── existing

.env.example                         UPDATED


============================================================
DOCUMENTATION
============================================================

Create:

docs/versions/v1.0.3.8.md

ACTION:
Create/replace the ENTIRE file with the following:

------------------------------------------------------------
# v1.0.3.8 — Structured Logging

## Objective

Establish structured JSON logging for Sadguru Catering OS.

## Completed

- Logger package
- Log levels
- Structured JSON logging
- LOG_LEVEL configuration
- Application logger integration
- HTTP request logging API
- Error logging integration
- Logger tests
- Runtime verification

## Log Levels

Supported:

- debug
- info
- warn
- error

Default:

info

## Log Format

Logs are emitted as structured JSON to stdout.

Example:

```json
{
  "level": "INFO",
  "msg": "application starting",
  "app": "Sadguru Catering OS",
  "version": "1.0.3",
  "environment": "development"
}# v1.0.3.8 — Structured Logging

## Objective

Establish structured JSON logging for Sadguru Catering OS.

## Completed

- Logger package
- Log levels
- Structured JSON logging
- LOG_LEVEL configuration
- Application logger integration
- HTTP request logging API
- Error logging API
- Logger tests
- Runtime verification

## Log Levels

Supported levels:

- debug
- info
- warn
- error

Default:

`info`

## Log Format

Logs are emitted as structured JSON to stdout.

Example:

```json
{
  "level": "INFO",
  "msg": "application starting",
  "app": "Sadguru Catering OS",
  "version": "1.0.3",
  "environment": "development"
}
